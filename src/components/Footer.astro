---
import { icons, navItems, social } from "../data";
import Logo from "./ui/Logo.astro";
import SectionTitle from "./ui/SectionTitle.astro";
---

<footer class="py-12">
  <div class="container px-4 mx-auto">
    <div class="flex w-full justify-between items-end">
      <div class="flex flex-col justify-between gap-8">
        <div class="space-y-4 text-neutral-100">
          <SectionTitle title="Contacto" />
          <p class="font-univers-ex text-2xl text-pretty">
            ¿Tenés un proyecto en mente?
            <br />
            Escribinos
          </p>
        </div>
        <div>
          {
            social.map((item) => {
              const Icon = icons[item.icon as keyof typeof icons];
              return (
                <a
                  href={item.url}
                  class="text-neutral-100 mr-4 w-10 inline-block"
                  target="_blank"
                  rel="noopener noreferrer"
                  aria-label={item.name}
                >
                  <Icon />
                </a>
              );
            })
          }
        </div>
      </div>
      <div class="flex items-end flex-col gap-6">
        <div class="text-white w-32">
          <Logo />
        </div>
        <nav>
          <ul class="text-white flex gap-4 items-center justify-end">
            {
              navItems.map((item) => (
                <li>
                  <a href={item.href}>{item.label}</a>
                </li>
              ))
            }
          </ul>
        </nav>
      </div>
    </div>
  </div>
</footer>
